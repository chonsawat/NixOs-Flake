{
  # ====================================================
  #  Toggle terminal
  # ----------------------------------------------------
  terminal.toggleterm.enable = true;
  terminal.toggleterm.setupOpts.direction = "float";
}
